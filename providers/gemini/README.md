# Gemini provider — not yet implemented

Phase 2 (subctl v0.2). The provider interface is in place; this directory
will house `auth.sh`, `signals.sh`, and `teams.sh` once the Gemini CLI's
auth and config-storage shape is mapped.

Tracking issue: https://github.com/webdevtodayjason/subctl/issues
