{
  "name": "subctl-dashboard",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "subctl dashboard — Bun HTTP+WebSocket server. Renders fleet state and exposes the web-terminal escape hatch (v2.7.21, ADR 0011 Layer 2).",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun run --watch server.ts"
  },
  "dependencies": {
    "lucide": "^0.474.0",
    "node-pty": "^1.0.0",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0"
  },
  "engines": {
    "bun": ">=1.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13"
  }
}
