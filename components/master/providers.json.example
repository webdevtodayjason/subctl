{
  "_comment": "Operator-editable model routing for clawd (subctl master). Roles are semantic — model names can change without code edits. Recommendation as of 2026-05: MLX runtime on Apple Silicon, with Ollama as fallback for models without MLX ports yet.",

  "models": {
    "router": {
      "_comment": "Fast, lightweight. Used for 'is this task simple enough for me?' routing decisions and tool-call dispatch. Should fit in <8GB RAM and respond <1s.",
      "provider": "lmstudio",
      "model": "google/gemma-4-e4b",
      "host": "http://localhost:1234/v1",
      "context_length": 8192
    },
    "supervisor": {
      "_comment": "Primary thinker. Conversational chat with operator, dev-team orchestration, Telegram digests. Reasoning models (qwen3.5/3.6 a3b) need 32K+ to fit system prompt + tool schemas + transcript + thinking budget. context_length is the critical knob — master force-loads at this size at boot via /api/v1/models/load to defeat LM Studio's 4K JIT default.",
      "provider": "lmstudio",
      "model": "qwen/qwen3.6-35b-a3b",
      "host": "http://localhost:1234/v1",
      "context_length": 65536
    },
    "reviewer": {
      "_comment": "Code-strong. PR diff review, deep code reasoning. 32K is plenty for one PR + master's system block.",
      "provider": "lmstudio",
      "model": "qwen/qwen3-coder-30b",
      "host": "http://localhost:1234/v1",
      "context_length": 32768
    },
    "embeddings": {
      "_comment": "Memory + vault search. Nomic ModernBERT, fast, 768-dim. context_length omitted — embedding models don't need a forced load size.",
      "provider": "lmstudio",
      "model": "text-embedding-nomic-embed-text-v1.5",
      "host": "http://localhost:1234/v1"
    },

    "_alt_supervisor_openrouter": {
      "_comment": "ALTERNATE EXAMPLE (v2.7.17) — point supervisor at OpenRouter for hundreds of models (incl. a free preview tier). To activate: rename this key to 'supervisor' (replacing the lmstudio block above), set openrouter_api_key in the dashboard's Settings → API Tokens panel (or export OPENROUTER_API_KEY in the launchd plist), then bounce master. Model IDs use vendor/name format — browse https://openrouter.ai/models for the live catalog. host is optional; when omitted, master defaults to https://openrouter.ai/api/v1.",
      "provider": "openrouter",
      "model": "anthropic/claude-sonnet-4",
      "context_length": 200000
    }
  },

  "escalate": {
    "_comment": "When local models aren't enough — irreversible decisions, complex multi-repo planning, hard reasoning. Verified working with argent.",
    "provider": "openai-codex",
    "model": "gpt-5.2",
    "auth": "oauth"
  },

  "_fallback_removed_2026_05_14": "NO ANTHROPIC FALLBACK. pi-agent-core is an Agent-SDK-shaped harness; provider=anthropic calls will bill against the $200/mo Agent SDK credit (Anthropic policy change, June 15 2026+), NOT against the operator's Max 20× subscription, regardless of any `auth: max-subscription` hint. Master's tick cadence (60s watchdog, 60s followup ticker, auto-compact, inbox poll) would burn the entire monthly credit in days and then start drawing extra usage charges. See ADR 0019 + DECISIONS.md (2026-05-14). If you genuinely need this and you've read both docs, set SUBCTL_ALLOW_ANTHROPIC_PROVIDER=1 in the launchd plist EnvironmentVariables — buildModel() emits a loud `[master][ANTHROPIC-API-GUARD]` log line + a Telegram alert + a dashboard notification on first construction so you can't miss it.",

  "routing_policy": {
    "_comment": "Defaults applied when the master_skill doesn't override. The master can self-elevate on a per-task basis via the `escalate_decision` tool.",
    "default": "supervisor",
    "code_review": "reviewer",
    "search_memory": "embeddings",
    "irreversible_decision": "escalate",
    "multi_repo_planning": "escalate"
  },

  "memory_budget_gb": {
    "_comment": "Total resident memory the master should target on the M3 Ultra (256GB total). Helps the boot probe decide which models to keep loaded vs unload between calls. ~50GB resident is comfortable.",
    "target": 50,
    "ceiling": 80
  }
}
